{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn api.index:app --reload",
  "functions": {
    "api/index.py": {
      "memory": 3008,
      "maxDuration": 60
    }
  },
  "env": {
    "PYTHONUNBUFFERED": "1"
  }
}
