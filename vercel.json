{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn vercel.api.index:app --reload",
  "functions": {
    "vercel/api/index.py": {
      "memory": 2048,
      "maxDuration": 60
    }
  }
}
