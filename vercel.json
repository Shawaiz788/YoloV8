{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn api.index:app --reload",
  "functions": {
    "api/index.py": {
      "memory": 2048,
      "maxDuration": 60
    }
  }
}
